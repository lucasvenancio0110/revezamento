(()=>{'use strict';
const BASE=['Lucas V.','Lucas R.','Marlon','Ewerson','Everson','Clayton','Wendel','Gabriel','Adriano','Luciano','Juliano','Nattan','Marcio','Christoffer','Patrício','Mateus','Alan','Shaiane','Willians'];
const SLOTS=['18:00','18:30','19:00','19:30','20:00','20:30'],END=1440,MOVE=10;
const state={step:0,roster:[...BASE],present:new Set(),activities:[],people:[],pi:0,ai:0,meals:{},covers:{},futureOwners:{}};
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)],norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase().trim(),uid=()=>Math.random().toString(36).slice(2),toMin=t=>{const[h,m]=String(t||'00:00').split(':').map(Number);return h*60+m},toTime=m=>`${String(Math.floor(m/60)).padStart(2,'0')}:${String(m%60).padStart(2,'0')}`,overlap=(a,b,c,d)=>Math.max(a,c)<Math.min(b,d);
function toast(x){const e=$('#toast');e.textContent=x;e.classList.add('show');clearTimeout(window.__t);window.__t=setTimeout(()=>e.classList.remove('show'),1600)}
function renderRoster(){$('#roster').innerHTML=state.roster.map(n=>`<button class="chip ${state.present.has(n)?'sel':''}" data-person="${n}">${n}</button>`).join('');$$('[data-person]').forEach(b=>b.onclick=()=>{state.present.has(b.dataset.person)?state.present.delete(b.dataset.person):state.present.add(b.dataset.person);renderRoster()});$('#count').textContent=`(${state.present.size})`}
function ownerOf(line){const u=norm(line);return [...state.present].sort((a,b)=>b.length-a.length).find(n=>u.includes(norm(n)))||null}
function parseReport(raw){let sec='ignore';const acts=[];for(const r of raw.split(/\n/)){const line=r.trim();if(!line)continue;const u=norm(line).replace(/\*/g,'');if(u==='SETUP'||u.includes('MAQUINAS EM SETUP')){sec='setup';continue}if(u.includes('MAQUINAS EM AJUSTES')||u==='AJUSTES'||u==='AJUSTES:'){sec='adjust';continue}if(u.includes('PROXIMOS SETUPS')){sec='future';continue}if(u.includes('MANUTENCAO')||u.includes('SETUPS 1')||u.includes('SETUPS 3')||u.includes('SELECAO')){sec='ignore';continue}if(line.includes('✅')||!['setup','adjust','future'].includes(sec))continue;const mm=line.match(/TNL\s*0*(\d{1,3})/i);if(!mm)continue;const machine=`TNL ${String(mm[1]).padStart(3,'0')}`,time=(line.match(/\b(\d{1,2}:\d{2})\b/)||[])[1]||'20:30';if(sec==='future'){acts.push({id:uid(),type:'future',machine,start:time});continue}const owner=ownerOf(line);if(owner)acts.push({id:uid(),type:sec,machine,owner,status:null,finish:null,coverTime:'20:30'})}state.activities=acts;state.people=[...new Set(acts.filter(a=>a.owner).map(a=>a.owner))];state.pi=0;state.ai=0;state.meals={};state.covers={};state.futureOwners={};return acts.length>0}
const personActs=n=>state.activities.filter(a=>a.owner===n),currentPerson=()=>state.people[state.pi]||null,currentAct=()=>{const p=currentPerson();return p?personActs(p)[state.ai]||null:null};
function busyUntil(name){const acts=personActs(name);if(!acts.length)return 1080;let max=1080;for(const a of acts){if(a.status==='finish'&&a.finish)max=Math.max(max,toMin(a.finish));else if(a.status==='cover'||a.status===null)return END}return max}
function events(name,ignore=null){const ev=[];if(state.meals[name])ev.push({s:toMin(state.meals[name]),e:toMin(state.meals[name])+60,label:'Jantar'});Object.entries(state.covers).forEach(([id,c])=>{if(id!==ignore&&c.who===name)ev.push({s:toMin(c.time),e:toMin(c.time)+60,label:`Cobre ${c.owner} — ${c.machine}`})});Object.entries(state.futureOwners).forEach(([id,n])=>{if(n===name){const f=state.activities.find(a=>a.id===id);if(f)ev.push({s:toMin(f.start),e:END,label:`Inicia ${f.machine}`})}});return ev}
function canDo(name,s,e,ignore=null){if(busyUntil(name)+MOVE>s)return false;return !events(name,ignore).some(x=>overlap(x.s,x.e,s,e))}
function mealSlot(name,deadline=END,extra=[]){if(state.meals[name])return state.meals[name];const free=busyUntil(name);return SLOTS.find(t=>{const s=toMin(t),e=s+60;return s>=free&&e<=deadline&&!events(name).concat(extra).some(x=>overlap(x.s,x.e,s,e))})||null}
function reserveFutures(preview=false){const out={...state.futureOwners};for(const f of state.activities.filter(a=>a.type==='future').sort((a,b)=>toMin(a.start)-toMin(b.start))){if(out[f.id])continue;const s=toMin(f.start),opts=[...state.present].filter(n=>canDo(n,s,END)&&mealSlot(n,s)).map(n=>({n,score:(busyUntil(n)<=1080?20:0)+events(n).length*10})).sort((a,b)=>a.score-b.score);if(opts[0])out[f.id]=opts[0].n}if(!preview)state.futureOwners=out;return out}
function candidatePlan(a){const s=toMin(a.coverTime),e=s+60,futures=reserveFutures(true),list=[];for(const n of state.present){if(n===a.owner||!canDo(n,s,e,a.id))continue;const meal=mealSlot(n,END,[{s,e}]);if(!meal)continue;let risk=0,why=[];for(const [id,who] of Object.entries(futures)){if(who===n){const f=state.activities.find(x=>x.id===id);if(f&&toMin(f.start)>=s){risk+=toMin(f.start)<1200?70:25;why.push(`pode ser necessário na ${f.machine} às ${f.start}`)}}}const alternatives=[...state.present].filter(x=>x!==n&&x!==a.owner&&canDo(x,s,e,a.id)).length;const score=300+Math.min(60,s-busyUntil(n))+alternatives*18-risk;list.push({name:n,score,meal,alternatives,risk,why})}return list.sort((a,b)=>b.score-a.score)}
function globalPanel(){const futures=state.activities.filter(a=>a.type==='future').sort((a,b)=>toMin(a.start)-toMin(b.start));const reserved=reserveFutures(true);const free=[...state.present].filter(n=>busyUntil(n)<=1080&&!Object.values(state.covers).some(c=>c.who===n));const later=[...state.present].map(n=>({n,t:busyUntil(n)})).filter(x=>x.t>1080&&x.t<END).sort((a,b)=>a.t-b.t).slice(0,5);return `<div class="factory"><div class="factory-title">MAPA VIVO DA FÁBRICA</div><div class="factory-grid"><div><span>LIVRES</span><strong>${free.join(', ')||'Nenhum'}</strong></div><div><span>LIBERAM</span><strong>${later.map(x=>`${x.n} ${toTime(x.t)}`).join(' • ')||'Aguardando ronda'}</strong></div></div><div class="future-strip">${futures.map(f=>`<div><time>${f.start}</time><span>${f.machine}</span><b>${reserved[f.id]?`→ ${reserved[f.id]}`:'⚠ aberto'}</b></div>`).join('')}</div></div>`}
function strategyHTML(a){const list=candidatePlan(a),best=list[0];if(!best)return `<div class="strategy danger"><b>Não há encaixe direto.</b><p>Continue a ronda ou altere o horário. O sistema manterá esta decisão pendente.</p></div>`;const futureText=best.why.length?best.why.join('; '):'preserva os próximos setups conhecidos';return `<div class="strategy"><div class="kicker">FAÇA ASSIM</div><h3>${best.name} cobre ${a.owner} às ${a.coverTime}</h3><div class="chain"><div>${best.name} cobre ${a.machine}</div><i>↓</i><div>${a.owner} janta ${a.coverTime}–${toTime(toMin(a.coverTime)+60)}</div><i>↓</i><div>${best.name} libera às ${toTime(toMin(a.coverTime)+60)}</div><i>↓</i><div>${futureText}</div></div><div class="impact-line"><span>${best.alternatives} alternativa(s) restantes</span><span>${best.risk?'Atenção futura':'Plano seguro'}</span></div><button class="btn primary full" data-confirm-cover="${best.name}">Confirmar estratégia</button><details><summary>Ver outras estratégias</summary>${list.slice(1,5).map(x=>`<button class="alt-btn" data-confirm-cover="${x.name}"><strong>${x.name}</strong><small>${x.why.length?x.why.join('; '):'Sem conflito futuro conhecido'}</small></button>`).join('')}</details></div>`}
function personTabs(){return `<div class="person-tabs">${state.people.map((p,i)=>`<button class="person-pill ${i===state.pi?'active':''} ${personActs(p).every(a=>a.status)?'done':''}" data-jump="${i}">${personActs(p).every(a=>a.status)?'✓ ':''}${p}</button>`).join('')}</div>`}
function renderRound(){const p=currentPerson(),a=currentAct();if(!p||!a){state.step=3;update();return}const done=state.activities.filter(x=>x.type!=='future'&&x.status).length,total=state.activities.filter(x=>x.type!=='future').length;$('#round').innerHTML=`${globalPanel()}${personTabs()}<div class="round-head"><div><span>AGORA</span><h2>${p}</h2><p>${done}/${total} decisões concluídas</p></div></div><div class="card decision-card"><div class="activity-head"><div><div class="machine">${a.type==='setup'?'🔴':'🟡'} ${a.machine}</div><div class="owner">${p}</div></div><span class="badge">${a.type==='setup'?'SETUP':'AJUSTE'}</span></div>${a.status?`<div class="locked">✓ ${a.status==='finish'?`Termina às ${a.finish} e janta ${state.meals[p]}`:`${state.covers[a.id]?.who} cobre às ${a.coverTime}`}</div><button class="btn secondary full" data-edit="1">Editar</button>`:`<div class="question">O que vai acontecer?</div><div class="choice"><button data-status="finish">Vai terminar</button><button data-status="cover">Precisa revezar</button></div>`}<div id="decision"></div></div>`;bindRound(a)}
function advance(){const p=currentPerson(),acts=personActs(p);if(state.ai<acts.length-1)state.ai++;else if(state.pi<state.people.length-1){state.pi++;state.ai=0}else{reserveFutures();state.step=3}update()}
function bindRound(a){$$('[data-jump]').forEach(b=>b.onclick=()=>{state.pi=+b.dataset.jump;state.ai=0;renderRound()});$('[data-edit]')&&($('[data-edit]').onclick=()=>{a.status=null;a.finish=null;delete state.covers[a.id];renderRound()});$$('[data-status]').forEach(b=>b.onclick=()=>{if(b.dataset.status==='finish'){$('#decision').innerHTML=`<div class="field"><label>Que horas termina?</label><input id="finish" class="input" type="time" value="19:00"></div><button id="calcFinish" class="btn primary full">Calcular plano deste preparador</button><div id="finishPlan"></div>`;$('#calcFinish').onclick=()=>{a.finish=$('#finish').value;const meal=mealSlot(a.owner)||'20:30';$('#finishPlan').innerHTML=`<div class="strategy"><div class="kicker">PLANO SUGERIDO</div><h3>${a.owner}</h3><div class="chain"><div>Termina ${a.machine} às ${a.finish}</div><i>↓</i><div>Janta ${meal}–${toTime(toMin(meal)+60)}</div><i>↓</i><div>Fica disponível para o restante do turno</div></div><button id="confirmFinish" class="btn primary full">Confirmar e próximo</button></div>`;$('#confirmFinish').onclick=()=>{a.status='finish';state.meals[a.owner]=meal;advance()}}}else{$('#decision').innerHTML=`<div class="field"><label>Horário do revezamento</label><input id="coverTime" class="input" type="time" value="20:30"></div><div id="rec"></div>`;const run=()=>{a.coverTime=$('#coverTime').value;$('#rec').innerHTML=strategyHTML(a);$$('[data-confirm-cover]').forEach(x=>x.onclick=()=>{a.status='cover';state.meals[a.owner]=a.coverTime;state.covers[a.id]={who:x.dataset.confirmCover,time:a.coverTime,owner:a.owner,machine:a.machine};advance()})};$('#coverTime').onchange=run;run()}})}
function allEvents(){reserveFutures();const rows=[];Object.entries(state.meals).forEach(([n,t])=>rows.push({n,s:toMin(t),text:`Jantar ${t}–${toTime(toMin(t)+60)}`}));Object.values(state.covers).forEach(c=>rows.push({n:c.who,s:toMin(c.time),text:`Revezar ${c.owner} — ${c.machine}`}));Object.entries(state.futureOwners).forEach(([id,n])=>{const f=state.activities.find(a=>a.id===id);if(f)rows.push({n,s:toMin(f.start),text:`Iniciar ${f.machine}`})});return rows.sort((a,b)=>a.s-b.s)}
function whatsapp(){const ev=allEvents(),by={};ev.forEach(x=>(by[x.n]||(by[x.n]=[])).push(x));let txt='*REVEZAMENTO*\n\n';for(const n of Object.keys(by)){txt+=`*${n.toUpperCase()}*\n`;for(const x of by[n])txt+=`${toTime(x.s)} - ${x.text}\n`;txt+='\n'}txt+='*PRÓXIMOS SETUPS*\n\n';for(const f of state.activities.filter(a=>a.type==='future').sort((a,b)=>toMin(a.start)-toMin(b.start)))txt+=`${f.start} - ${f.machine} - ${state.futureOwners[f.id]||'SEM RESPONSÁVEL'}\n`;const unresolved=state.activities.filter(a=>a.type!=='future'&&!a.status);txt+='\n*PENDÊNCIAS*\n';txt+=unresolved.length?unresolved.map(a=>`${a.machine} - ${a.owner}`).join('\n'):'Nenhuma';return txt}
function renderFinal(){const ev=allEvents(),by={};ev.forEach(x=>(by[x.n]||(by[x.n]=[])).push(x));const unresolved=state.activities.filter(a=>a.type!=='future'&&!a.status);$('#final').innerHTML=`<div class="validation ${unresolved.length?'warnbox':'okbox'}"><strong>${unresolved.length?'⚠ Plano com pendências':'✓ Plano validado'}</strong><span>${unresolved.length?`${unresolved.length} decisão(ões) faltando`:'Todas as decisões conhecidas foram consideradas'}</span></div><div class="report-actions"><button id="copyWA" class="btn primary full">📋 Copiar relatório para WhatsApp</button><button id="showText" class="btn secondary full">Ver texto completo</button></div>${Object.entries(by).map(([n,rows])=>`<div class="card person-report"><h3>${n}</h3>${rows.map(x=>`<div class="report-line"><time>${toTime(x.s)}</time><span>${x.text}</span></div>`).join('')}</div>`).join('')}<textarea id="waText" class="wa-text" hidden>${whatsapp()}</textarea>`;$('#copyWA').onclick=async()=>{const t=whatsapp();try{await navigator.clipboard.writeText(t);toast('Relatório copiado')}catch{const ta=$('#waText');ta.hidden=false;ta.value=t;ta.select();document.execCommand('copy');toast('Relatório copiado')}};$('#showText').onclick=()=>{const ta=$('#waText');ta.hidden=!ta.hidden;ta.value=whatsapp()}}
function update(){$$('.step').forEach((e,i)=>e.classList.toggle('active',i===state.step));$$('.progress span').forEach((e,i)=>e.classList.toggle('on',i<=state.step));$('#back').style.visibility=state.step===0?'hidden':'visible';$('#next').textContent=state.step===2?'Ver relatório':state.step===3?'Voltar à ronda':'Continuar';if(state.step===2)renderRound();if(state.step===3)renderFinal();scrollTo(0,0)}
$('#addName').onclick=()=>{const i=$('#newName'),v=i.value.trim();if(!v)return;let n=state.roster.find(x=>norm(x)===norm(v));if(!n){n=v;state.roster.push(n)}state.present.add(n);i.value='';renderRoster()};$('#example').onclick=()=>$('#report').value='*MAQUINAS EM SETUP*\n🔴 TNL 053 - NATTAN\n🔴 TNL 076 - CLAYTON\n\n*MAQUINAS EM AJUSTES*\nTNL 033 - EWERSON\n\n*PRÓXIMOS SETUPS*\n🔴 TNL 112 - Setup 2°T (19:30)';$('#parse').onclick=()=>{if(!parseReport($('#report').value)){toast('Nenhuma atividade válida');return}state.step=2;update()};$('#back').onclick=()=>{if(state.step>0){state.step--;update()}};$('#next').onclick=()=>{if(state.step===0&&state.present.size<2){toast('Selecione ao menos 2 preparadores');return}if(state.step===1){if(!parseReport($('#report').value)){toast('Cole um relatório válido');return}state.step=2}else if(state.step===2)state.step=3;else if(state.step===3)state.step=2;else state.step++;update()};renderRoster();update();})();